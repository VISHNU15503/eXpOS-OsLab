int main()
{
    decl

        int permission,temp;
        str filename,enter,p;

    enddecl
    begin
    	enter = "Enter : ";
    	p = "P : ";
    	temp = exposcall ( "Write" , -2, enter );
        read(filename);
        temp = exposcall ( "Write" , -2, p );
        read(permission);
        temp = exposcall("Create",filename,permission);
        return 0;
    end
}
